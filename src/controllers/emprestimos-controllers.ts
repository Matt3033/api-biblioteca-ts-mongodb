import { Request, Response } from 'express';

export default class EmprestimosControllers{
    public async cadastrarEmprestimoController(req: Request, res: Response): Promise<Response | void> {
        
    }
}